<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Validar Supermercados — Admin</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
</head>
<body class="container py-4">
  <nav class="mb-4">
    <a href="/dashboard" class="btn btn-outline-secondary"><i class="bi bi-arrow-left"></i> Dashboard</a>
  </nav>

  <h1 class="h3 mb-4"><i class="bi bi-shop"></i> Validar Supermercados Pendentes</h1>

  <div class="card shadow-sm">
    <div class="card-body p-0">
      <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
          <thead class="table-light">
            <tr>
              <th>Nome da Empresa</th>
              <th>Email de Contacto</th>
              <th>Distrito</th>
              <th>Cidade</th>
              <th class="text-end">Ação</th>
            </tr>
          </thead>
          <tbody>
            <% if (pendingSupermarkets.length === 0) { %>
              <tr>
                <td colspan="5" class="text-center py-4 text-muted">Não existem supermercados pendentes de aprovação.</td>
              </tr>
            <% } else { %>
              <% pendingSupermarkets.forEach(user => { %>
                <tr>
                  <td><strong><%= user.name %></strong></td>
                  <td><%= user.email %></td>
                  <td><%= user.district || 'N/I' %></td>
                  <td><%= user.city || 'N/I' %></td>
                  <td class="text-end">
                    <form action="/admin/approve-supermarket/<%= user._id %>" method="POST" class="d-inline">
                      <button type="submit" class="btn btn-sm btn-success"><i class="bi bi-check-lg"></i> Aprovar</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>
</html>